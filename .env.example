# Copy to .env (which is git-ignored), then run `docker compose up`:  cp .env.example .env
# These are throwaway LOCAL-DEV values — put real/production secrets only in .env, never here.
POSTGRES_DB=reefpulse
POSTGRES_USER=reefpulse
POSTGRES_PASSWORD=reefpulse
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
